name: 'Commit Message Enhancer'
description: 'Generates a clean commit message from diffs using Gemini LLM'
inputs:
  gemini-api-key:
    description: 'API key for Google Gemini'
    required: true
runs:
  using: 'node20'
  main: 'dist/index.js'