{
    "name": "papertrail",
    "version": "0.0.9",
    "description": "Generates clean commit messages from diffs using Gemini LLM",
    "main": "index.js",
    "scripts": {
        "build": "ncc build index.js -o dist --external @actions/github --license licenses.txt",
        "test": "echo 'Tests TBD'"
    },
    "dependencies": {
        "@actions/core": "1.10.1",
        "@actions/github": "5.1.0",
        "@google/generative-ai": "0.20.0"
    },
    "devDependencies": {
        "@vercel/ncc": "0.38.1"
    }
}